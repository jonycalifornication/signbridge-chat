version: '3.8'

services:
  avatar-widget:
    build:
      context: .
      target: builder
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: >
      sh -c "npm install && npm run dev -- --host"
