{
  "name": "signbridge-video-generator",
  "version": "1.0.0",
  "description": "Sign language video generator. Renders on this GPU box; the avatar itself is mirrored from the signBridge-avatar project.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "server": "node src/server/index.js",
    "render": "node src/headless/render.js",
    "avatar:sync": "node scripts/sync-avatar.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "puppeteer": "^24.34.0",
    "webm-muxer": "^5.0.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.15",
    "@vitest/ui": "^4.0.15",
    "happy-dom": "^20.0.11",
    "vite": "^5.4.0",
    "vitest": "^4.0.15"
  }
}
